### basic settings

# site properties and page defaults
title: Bio-Physics Laboratory
[subtitle: at the NIT Patna]: #
description: Biological · Physics · Nanoscale
logo: images/logo_white.svg
[logo: images/lab-6.png]: #
logo-text: true
header: none
header-dark: false
footer: none
baseurl: /

# site social media and other links
links:
  email: subrata@nitp.ac.in
  google-scholar: https://scholar.google.co.in/citations?user=uj0Xb94AAAAJ&hl=en
  github: smphys
  link:

### advanced settings

# automatic citations
auto-cite:
  plugins:
    - name: sources
      input:
        - ../_data/sources.yaml
  output: ../_data/citations.yaml

# default front matter parameters for markdown files
defaults:
  # all markdown files
  - scope:
      path: ""
    values:
      layout: default
  # markdown files in /_members
  - scope:
      type: "members"
    values:
      layout: member
  # markdown files in /_posts
  - scope:
      type: "posts"
    values:
      layout: post

# collections
collections:
  members:
    output: true

# sass settings
sass:
  sass_dir: css

# syntax highlighting
highlighter: rouge

# plugins
plugins:
  - jekyll-redirect-from
  - jekyll-feed
  - jekyll-sitemap
  # - jekyll-github-metadata
  # - jekyll-avatar
  # - jekyll-gist
  # - jekyll-mentions
  # - jekyll-relative-links
  # - jemoji

# jekyll theme
theme: null
